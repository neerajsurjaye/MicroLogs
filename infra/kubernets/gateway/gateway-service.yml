apiVersion: v1
kind: Service
metadata:
    name: gateway-service
spec:
    type: NodePort
    selector:
        app: gateway-deployment
    ports:
        - port: 8080
          targetPort: 8080
          nodePort: 30570
